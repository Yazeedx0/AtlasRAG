meta {
  name: Deactivate Principal - Destructive
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/v1/iam/principals/{{targetPrincipalId}}/deactivate
  body: none
  auth: inherit
}

tests {
  test("deactivates the target principal", function () {
    expect(res.getStatus()).to.equal(204);
  });
}

docs {
  Requires `iam.principals.manage`. This removes active access for `targetPrincipalId`.
}
